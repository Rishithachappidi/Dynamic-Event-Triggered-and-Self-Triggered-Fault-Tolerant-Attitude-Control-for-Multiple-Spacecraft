# Output Images

This directory is the **single output-image location** for the project.

It contains the supplied reproduced figures `Fig02.png` through `Fig15.png`.

Running:

```matlab
R = sff_run_all;
```

from the `simulation/` directory will save regenerated figures back into this folder.

These are project simulation outputs. They are not copied image files from the IEEE paper.
